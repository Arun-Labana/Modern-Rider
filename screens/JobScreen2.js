import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import { SafeAreaView } from 'react-native-safe-area-context'

const JobScreen2 = () => {
  return (
    <SafeAreaView>
        <View>
            <Text style={{margin : 10, marginTop : 50, fontSize : 50}}>Please pickup user from Surathkal. He is waiting for you.</Text>
        </View>
    </SafeAreaView>
  )
}

export default JobScreen2

const styles = StyleSheet.create({})